llm:
  model_name: "gemma4:31b"               # 必须是 Ollama 里显示的名字
  base_url: "http://localhost:11434/v1" # Ollama 的原生 OpenAI 兼容端口
  api_key: "ollama"                  
  temperature: 0.15
  max_tokens: 2048

agent:
  max_hops: 3

retriever:
  model_path: "/root/autodl-tmp/bge-m3" 
  top_k: 10
  
data:
  graph_cache_path: "data/cache/job_dict_graph.pkl"